version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: FIT5120_PROJECT/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*  
